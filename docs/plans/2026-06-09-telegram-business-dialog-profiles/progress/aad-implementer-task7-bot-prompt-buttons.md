## Task 7 bot prompt buttons progress — 2026-06-10

- Startup: loaded delegated dispatch, AGENTS/CLAUDE guidance, task package/report instructions, backend quality skill, and plan Task 7 scope.
- Dirty state reviewed with explicit authorization: preserving existing Task 8 content in root `progress.md`; leaving owner `progress/slice-owner-task7.md` intact; updating previous blocked Task 7 report path in place later.
- Initial inspection found existing patterns in `gateway/platforms/telegram.py`: `_business_mode_keyboard`, `bm:r:<token>` add-rule flow, `_business_pending_rule_tokens`, `_maybe_handle_business_rule_text`, `_is_callback_user_authorized`, `_business_profile_store_obj`, and `_record_business_history_event`.
- TDD plan: add focused tests in `tests/gateway/test_telegram_business.py` for keyboard buttons, authorized prompt save, clear prompt/history, unauthorized callback rejection, cancel without save, and add-rule regression; then implement minimal Telegram adapter changes and run container-only verification.
- RED check: `docker run --rm -v "$PWD":/workspace -w /workspace -e HERMES_TEST_VENV=/opt/hermes-test-venv -e HERMES_TEST_WORKERS=4 -e TZ=UTC -e LANG=C.UTF-8 -e LC_ALL=C.UTF-8 -e PYTHONHASHSEED=0 hermes-agent:test-runner scripts/run_tests.sh tests/gateway/test_telegram_business.py -- -q -k 'business_mode_keyboard_contains_prompt_controls or business_prompt_button_stores_next_owner_text_as_dialog_prompt or business_prompt_cancel_does_not_save_dialog_prompt or business_clear_prompt_button_writes_empty_prompt_and_history or business_prompt_callback_rejects_unauthorized_user_without_pending_prompt or business_add_rule_button_stores_next_owner_text_as_notify_rule'` failed as expected: missing prompt buttons/pending prompt state and clear callback behavior; unauthorized prompt callback and add-rule regression still passed.
- GREEN focused check: same container command passed (`tests/gateway/test_telegram_business.py` selected Task 7 tests) — 6 passed. Implemented prompt keyboard controls, authorized pending prompt save/cancel, clear prompt DB write/history, unauthorized rejection, and add-rule regression coverage.
- Before broader verification: running full Telegram Business gateway tests plus settings/profile-adjacent tests in the existing container image; no host pytest/npm/uv/build commands.
- Broader container verification passed: `docker run --rm -v "$PWD":/workspace -w /workspace -e HERMES_TEST_VENV=/opt/hermes-test-venv -e HERMES_TEST_WORKERS=4 -e TZ=UTC -e LANG=C.UTF-8 -e LC_ALL=C.UTF-8 -e PYTHONHASHSEED=0 hermes-agent:test-runner scripts/run_tests.sh tests/gateway/test_telegram_business.py tests/gateway/test_telegram_business_dashboard_api.py tests/gateway/test_telegram_business_profiles.py -- -q` — 3 files / 136 tests passed.
- Quality checks passed: container `py_compile` for touched Python files; container `ruff check gateway/platforms/telegram.py gateway/platforms/telegram_business_history.py tests/gateway/test_telegram_business.py`.
- Preparing report/commit. Note: root `progress.md` contains pre-existing unrelated Task 8 dirty content; I appended Task 7 updates as requested but will not include root `progress.md` in the Task 7 commit to avoid committing unrelated Task 8 content.
- Commit created: `e4acd6ab3 feat(telegram-business): add bot prompt controls`.
- Final report updated with commit SHA; preparing final evidence commit for report/progress SHA update only.
